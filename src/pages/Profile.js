import React from "react";

const Profile = () => {
  return (
    <div style={styles.container}>
      <h2>프로파일 페이지 입니다</h2>
    </div>
  );
};

const styles = {
  container: {
    padding: "20px",
    textAlign: "center",
  },
};

export default Profile;
